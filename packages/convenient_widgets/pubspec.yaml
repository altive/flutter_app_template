name: convenient_widgets
description: Convenient Widgets.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.5
  simple_logger: ^1.8.0+1

dev_dependencies:
  altive_lints: ^1.14.1
  build_runner: ^2.4.9
  custom_lint: ^0.6.7
  flutter_gen_runner: ^5.3.1
  flutter_test:
    sdk: flutter

flutter_gen:
  enabled: true
  output: lib/gen/
  line_length: 80
  integrations:
    flutter_svg: true
    flare_flutter: false
    rive: false
    lottie: false
  assets:
    outputs:
      style: dot-delimiter

flutter:
  assets:
    - assets/
