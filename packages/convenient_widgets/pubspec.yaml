name: convenient_widgets
description: Convenient Widgets.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17
  simple_logger: ^1.8.0+1

dev_dependencies:
  altive_lints: ^1.19.1
  build_runner: ^2.4.15
  custom_lint: ^0.7.0
  flutter_gen_runner: ^5.10.0
  flutter_test:
    sdk: flutter

flutter_gen:
  integrations:
    image: true
    flutter_svg: true
    rive: false
    lottie: false

flutter:
  assets:
    - assets/
