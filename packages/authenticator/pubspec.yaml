name: authenticator
description: Authenticator.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.1.2

dependencies:
  collection: ^1.16.0
  firebase_auth: ^4.7.3
  flutter:
    sdk: flutter
  google_sign_in: ^6.1.0
  riverpod: ^2.3.7
  riverpod_annotation: ^2.1.2

dev_dependencies:
  altive_lints: ^1.8.1
  build_runner: ^2.4.6
  flutter_test:
    sdk: flutter
  riverpod_generator: ^2.2.6

flutter:
