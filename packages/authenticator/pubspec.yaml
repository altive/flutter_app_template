name: authenticator
description: Authenticator.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  collection: ^1.16.0
  firebase_auth: ^4.2.6
  flutter:
    sdk: flutter
  google_sign_in: ^6.0.1
  riverpod: ^2.3.0
  riverpod_annotation: ^2.0.0

dev_dependencies:
  altive_lints: ^1.6.1
  build_runner: ^2.3.2
  flutter_test:
    sdk: flutter
  riverpod_generator: ^2.0.0

flutter:
