name: authenticator
description: Authenticator.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  collection: ^1.16.0
  firebase_auth: ^3.7.0
  flutter:
    sdk: flutter
  google_sign_in: ^5.4.1
  riverpod: ^2.0.0-dev.9

dev_dependencies:
  altive_lints: ^1.5.0
  flutter_test:
    sdk: flutter

flutter:
