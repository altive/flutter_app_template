name: themes
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  altive_lints: ^1.19.1
  custom_lint: ^0.7.0
  flutter_test:
    sdk: flutter
