name: themes
description: A new Flutter package project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  altive_lints: ^1.10.0
  flutter_test:
    sdk: flutter

flutter:
