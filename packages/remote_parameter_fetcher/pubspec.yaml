name: remote_parameter_fetcher
description: Fetch parameters set remotely.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.1.2

dependencies:
  firebase_remote_config: ^4.2.5
  flutter:
    sdk: flutter
  meta: ^1.7.0
  riverpod: ^2.3.7
  riverpod_annotation: ^2.1.2
  version: ^3.0.2

dev_dependencies:
  altive_lints: ^1.8.1
  build_runner: ^2.4.6
  mockito: ^5.3.0
  riverpod_generator: ^2.2.6
  test:
